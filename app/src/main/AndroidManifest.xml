<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hyte.fysio">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FeedBackActivity"></activity>
        <activity android:name=".DisclaimerActivity" />
        <activity android:name=".HomeFrontActivity" />
        <activity android:name=".InfoActivity" />
        <activity
            android:name=".MusclesList"
            android:parentActivityName=".HomeBackActivity">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".HomeBackActivity" />
        </activity>
        <activity
            android:name=".ShowExerciseActivity"
            android:parentActivityName=".HomeFrontActivity">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".HomeFrontActivity" />
        </activity>
        <activity android:name=".HomeBackActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>